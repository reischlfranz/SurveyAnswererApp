<?xml version="1.0"
      encoding="utf-8"?>

<ContentView xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             xmlns:style="clr-namespace:SurveyAnswererApp.Views.Style;assembly=SurveyAnswererApp"
             x:Class="SurveyAnswererApp.Views.AnswerSingleChoiceView">
  <ContentView.Resources>
    <style:TextStyleResource />
  </ContentView.Resources>
  <ContentView.Content>
    <StackLayout Orientation="Horizontal">


      <RadioButton Text="{Binding Answer.AnswerText}"
                   IsChecked="{Binding SingleChoiceAnswerSelection}"
                   GroupName="{Binding RadioButtonGroupName}"
                   HorizontalOptions="FillAndExpand" />

    </StackLayout>
  </ContentView.Content>
</ContentView>
